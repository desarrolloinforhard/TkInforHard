# Naming Conventions

Convenciones:

- Componentes publicos empiezan con `IH`.
- Archivos en `snake_case`.
- Clases en `PascalCase`.
- Variants en minuscula: `primary`, `success`, `warning`.
- Estilos ttk con prefijo `IH.`.

