# Tkinter Patterns

Patrones utiles:

- Usar `StringVar`, `BooleanVar` y variables Tk para estado simple.
- Mantener controladores fuera de widgets cuando hay logica de negocio.
- Usar `pack`, `grid` y `place` con criterio; no mezclarlos dentro del mismo contenedor padre.

