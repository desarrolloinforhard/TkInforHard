# Responsive UI

Tkinter no tiene responsive layout web. TkInforHard usa:

- `grid` con pesos.
- Scrollframes.
- Buckets por ancho (`compact`, `medium`, `wide`).
- Componentes que se expanden horizontalmente.

