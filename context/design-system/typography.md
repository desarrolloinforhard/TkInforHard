# Typography

La tipografia vive en `TkInforHard/theme/typography.py`.

Fuente base:

- `Segoe UI` para UI general.
- `Consolas` para contenido tecnico o monoespaciado.

Los estilos `IH.TLabel`, `IH.Title.TLabel` y `IH.CardTitle.TLabel` aplican la escala tipografica.

